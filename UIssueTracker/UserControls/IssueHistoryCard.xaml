<UserControl x:Class="UIssueTracker.UserControls.IssueHistoryCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             Background="{DynamicResource MaterialDesignPaper}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="pack://application:,,,/MaterialDesignThemes.Wpf;component/Resources/Roboto/#Roboto"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <materialDesign:Card Grid.Row="1" Margin="5" materialDesign:ShadowAssist.ShadowDepth="Depth3">
        <materialDesign:ColorZone Margin="6" Padding="5">
            <StackPanel>
                <TextBlock x:Name="CardTitle" Margin="4">ISSUE NUMBER</TextBlock>
                <Button x:Name="RemoveFromHistory_Btn" Margin="4,0,0,0"  Padding="0" Style="{StaticResource MaterialDesignFlatButton}" HorizontalAlignment="Left" Click="RemoveFromHistory_Btn_Click">REMOVE FROM HISTORY</Button>
            </StackPanel>
        </materialDesign:ColorZone>
    </materialDesign:Card>
</UserControl>
